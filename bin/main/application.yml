spring:
    application:
        name: caring
    profiles:
      active: dev

# 🌐 서버 설정
server:
  port: 8080
  servlet:
    context-path: /

# 📝 로깅 설정
logging:
  level:
    com.caring.caringbackend: INFO
    org.springframework.web: DEBUG
    p6spy: INFO  # P6Spy 로그 레벨
    com.caring.caringbackend.global.aop: INFO  # AOP 로깅

# Kakao Map API
kakao:
  api:
    key: ${KAKAO_API_KEY}

# P6Spy 설정
decorator:
  datasource:
    p6spy:
      enable-logging: true

# 📚 Swagger/OpenAPI 설정
springdoc:
  swagger-ui:
    tags-sorter: alpha  # 태그를 알파벳 순으로 정렬 (번호를 앞에 붙여서 순서 제어)
    # operations-sorter는 커스텀 JS로 처리 (summary 번호 순서)
    custom-js-url: /swagger-custom.js  # 커스텀 정렬 스크립트

