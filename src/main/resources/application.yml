spring:
    application:
        name: caring
    profiles:
      active: dev

# 🌐 서버 설정
server:
  port: 8080
  servlet:
    context-path: /

# 📝 로깅 설정
logging:
  level:
    com.caring.caringbackend: INFO
    org.springframework.web: DEBUG
