spring:
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:3306/caring?useSSL=false&allowPublic
        username: root
        password: na58745874@
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true

server:
    port: 8080

logging:
    level:
        root: INFO
